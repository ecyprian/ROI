import { InputSelectAllTextDirective } from './input-select-all-text.directive';

describe('InputSelectAllTextDirective', () => {
  it('should create an instance', () => {
    const directive = new InputSelectAllTextDirective();
    expect(directive).toBeTruthy();
  });
});
